# wick.and.ledger
Single-file costing and inventory ledger for a small candle studio. Price recipes from real material costs, size batches against what's actually on the shelf, and log sales that record true margin instead of estimates. One HTML file — no build, no server, no account. Your data is a JSON file you save, load, and own.
